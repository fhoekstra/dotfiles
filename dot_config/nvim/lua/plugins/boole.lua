return {
  "nat-418/boole.nvim",
  opts = {
    mappings = {
      increment = "<C-a>",
      decrement = "<C-x>",
    },
    additions = {
      -- Example additions (can be customized further)
      -- { "foo", "bar" },
      -- { "tic", "tac", "toe" },
    },
  },
}
