" indentation
set expandtab
set shiftwidth=2
set tabstop=2

syntax on
filetype indent plugin on

set ruler

" line numbers
set relativenumber
set number

" search higlighting
set is " incremental search
set hls " highlight search

" Ctrl+S to save
noremap <silent> <C-S>          :update<CR>
vnoremap <silent> <C-S>         <C-C>:update<CR>
inoremap <silent> <C-S>         <C-O>:update<CR>

" soft-wrap text (only visually) at the edge of the window/pane
set textwidth=0
set wrapmargin=0
set wrap
set linebreak " (optional - breaks by word rather than character)
